# Backend base URL for API requests. Leave unset for local dev - Vite's
# proxy (vite.config.js) routes "/api" to the local backend. Set this to the
# deployed backend's public URL for a production build
# (e.g. VITE_API_BASE_URL=https://netra-backend.onrender.com).
VITE_API_BASE_URL=
